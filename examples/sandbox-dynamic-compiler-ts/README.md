# Solari Dynamic Compiler Sandbox (TypeScript)

Demonstrates how to run dynamic multi-language code compilation, AST parsing, and performance benchmarking safely inside an isolated Solari MicroVM Sandbox.

## Setup & Run

```bash
cp .env.example .env
# Add your SOLARI_API_KEY
npm install
npm start
```
